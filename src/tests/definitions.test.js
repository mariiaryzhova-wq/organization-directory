export const OrganizationStatus = {
    pending: 'pending',
    approved: 'approved',
    rejected: 'rejected',
    archived: 'archived',
};
test('Мой четвертый проверочный тест', () => {
  expect(1 + 1).toBe(2);
});